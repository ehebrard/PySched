%\documentstyle{article}\documentclass{article}\usepackage[round]{natbib}   % omit 'round' option if you prefer square brackets\bibliographystyle{plainnat}\usepackage{latexsym}\usepackage[dvips]{graphicx}\usepackage[dvips]{color}\usepackage{algorithm2e}\usepackage{amsmath}\newlength{\halftextwidth}\setlength{\halftextwidth}{0.47\textwidth}\def\halffigsize{2.75in}\def\negvspace{0in}\def\posvspace{0em}\def\msetl{\mbox{$\{ \! \{$}}\def\msetr{\mbox{$\} \! \}$}}\input epsf\newcommand{\constraint}[1]{\mbox{\sc #1}}\newcommand{\algo}[1]{\mbox{\texttt{#1}}}\def\connect{\constraint{Connectivity}}\def\pconnect{\constraint{2D-Connectivity}}\def\sconnect{\constraint{Single-Shape-Connectivity}}\def\dc{\mbox{\sc DC}}\newcommand{\ub}[1]{\bar{#1}}\newcommand{\lb}[1]{\b{#1}}\newcommand{\dom}[1]{{\cal D}({#1})}\newtheorem{observation}{Observations}\newcommand{\dur}[1]{\ensuremath{p_{#1}}}\newcommand{\rate}[1]{\ensuremath{r_{#1}}}\def\capchar{q}\newcommand{\capacity}[1]{\ensuremath{\capchar_{{#1}}}}\newcommand{\population}[1]{\ensuremath{w_{#1}}}\def\parchar{p}\newcommand{\parent}[1]{\ensuremath{\parchar({#1})}}\newcommand{\antecedants}[1]{\ensuremath{\hat{\parchar}({#1})}}\def\childchar{C}\newcommand{\children}[1]{\ensuremath{\childchar({#1})}}\newcommand{\descendants}[1]{\ensuremath{\hat{\childchar}({#1})}}\newcommand{\leafs}[1]{\ensuremath{L({#1})}}\newcommand{\delay}[1]{\ensuremath{\delta_{#1}}}\newcommand{\limit}[1]{\ensuremath{e_{#1}}}\newcommand{\flowf}[1]{\ensuremath{\phi_{#1}}}\newcommand{\flow}[2]{\ensuremath{\flowf{#1}({#2})}}\newcommand{\length}[2]{\ensuremath{l_{{#1}{#2}}}}\def\graph{\ensuremath{{\cal G}}}\def\nodes{\ensuremath{{\cal N}}}\def\enodes{\ensuremath{{\cal E}}}\def\tnodes{\ensuremath{{\cal T}}}\def\snodes{\ensuremath{{\cal S}}}\def\arcs{\ensuremath{{\cal A}}}%\addtolength{\textwidth}{1.5in}%\addtolength{\oddsidemargin}{-0.75in}%\addtolength{\topmargin}{-0.5in}%\addtolength{\textheight}{0.5in}\begin{document}\begin{tabbing}ROC-Note 2  \` by Emmanuel\\Limited Circulation\footnotemark \`\today\\\end{tabbing}\begin{LARGE}\begin{center}\footnotetext{Do not let this note fall into the wrong hands!} {\bf Resource Constraints Algorithms Bibliography}\end{center}\end{LARGE}\section*{Unary Resources}\subsection*{Disjunctive}\begin{itemize}	\item Jackson's preemptive schedule as lower bound in \cite{Carlier82} and statement of the ``overload checking'' rule	\item Disjunctive reasoning in \cite{CarlierPinson89}, as an easy subset of the edge finding rules\end{itemize}\subsection*{Edge Finding}\begin{itemize}	\item Edge finding rules for preemptive scheduling described in \citep{CarlierPinson89}	\item Edge finding based on Jackson's preemptive schedule in \citep{CarlierPinson90} for preemptive scheduling	\item Brute-force (exponential time) edge finding for non-preemptive scheduling \citep{ApplegateCook91}	\item Range variables and Edge Finding propagation within a constraint toolkit in \citep{CaseauLaburthe94}\end{itemize}% \paragraph{Overload Checking.}\paragraph{Edge Finding.}\begin{itemize}	\item Edge finding rules in $O(n \log n)$ in \citep{CarlierPinson94}	\item Algorithm easier to implement in $O(n^2)$ in \citep{MartinShmoys96} and by Wim Nuijten, published in \citep{BaptisteEtAl01} 	\item Simpler algorithm in $O(n \log n)$ in \citep{VilimEtAl04}\end{itemize}\paragraph{Not First / Not Last.}\begin{itemize}	\item Rules in \citep{PinsonPhD}	\item Not-first/Not-last rules in $O(n^2)$ algo in \citep{BaptisteLePape96b}	\item Simpler algorithm in \citep{TorresLopez00}	\item Algorithm in $O(n \log n)$ in \citep{Vilim04}\end{itemize}\paragraph{Detectable Precedences.}\begin{itemize}	\item Algorithm in $O(n \log n)$ in \citep{Vilim04}\end{itemize}\section*{Cumulative Resources}\begin{itemize}	\item Constraint cumulative introduced in \citep{AggounBeldiceanu93}, but no mention to an algorithm\end{itemize}\subsection*{Timetabling}\begin{itemize}	\item Notion of ``compulsory part'' in \cite{Lahrichi82}	\item Already in Le Pape's thesis \cite{LePapePhD}	\item ``Resource profile'' in \cite{Fox90} sketch of algorithm which seems complete. No complexity given, no sorting mentioned, so probably $O(n^2)$	\item Implementation in Ilog Scheduler \cite{LePape95}	\item Usage in CP \cite{CaseauLaburthe96} ``resource histogram'' algorithm is not complete	\item Sweep algorithm \cite{BeldiceanuCarlsson01}, generalised in several ways	% \item Timetabling algo in \cite{BaptisteLePape96}\end{itemize}\subsection*{Edge Finding}\paragraph{Edge Finding}\begin{itemize}	\item Algorithm in $O(n^2k)$ in \citep{NuijtenPhD} ($k$ is the number of distinct resource demands)	\item Algorithm in $O(n^2)$ in \citep{BaptisteEtAl01}	\item Algorithm in $O(n \log n)$ in \citep{Vilim09} (Theta trees)\end{itemize}\paragraph{Not-first/not-last}\begin{itemize}	\item Algorithm in $O(n^2 \log n)$ in \citep{SchuttWolf10}\end{itemize}\paragraph{Extended Edge Finding.}\begin{itemize}	\item Algorithm in $O(n^3)$ in \citep{NuijtenPhD} (or is it in 95?)	\item Algorithm in $O(n^2k)$ in \citep{MercierVanHentenryck05}\end{itemize}\paragraph{Timetable Edge Finding.}\begin{itemize}	\item Algorithm in $O(kn \log n)$ in \citep{Vilim11}	\item Algorithm in $O(n^2)$ in \citep{KameugneEtAl11}\end{itemize}\paragraph{Timetable Edge Finding.}\begin{itemize}	\item Algorithm in $O(kn \log n)$ in \citep{OuelletQuimper13}\end{itemize}\subsection*{Energetic Reasoning}\section*{Survey}\begin{itemize}	\item \citep{LombardiMilano12}\end{itemize} \bibliography{src/biblio/index.bib}\end{document}